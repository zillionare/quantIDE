# 26-04-01

## User

- Approved executing the current task order in `.dev/tasks.md`.
- Required that after all tasks finish, `.dev/tasks.md` be moved to `.dev/archived/{yy-mm-dd}-tasks.md`, and a new blank `.dev/tasks.md` be prepared for the next task batch.
- Asked to create a commit for the completed work before continuing.

## Assistant

- Re-read the current `.dev/tasks.md` and the updated `.dev/rules/AGENTS.MD`, including the new rule 10 about archiving tasks after completion.
- Started executing tasks in order.
- Completed `P0 收紧无 gateway 时的仿真与实盘入口` by enforcing feature gates in `quantide/web/middleware_feature.py` for `/trade/simulation` and `/trade/live`.
- Added focused trade-page tests for disabled gateway behavior and validated them with `pytest tests/web/test_trade.py`.
- Completed `P0 补齐 Init Wizard 的服务端必填校验` by adding required-field validation for `gateway_api_key` and `tushare_token` in both the wizard UI flow and `InitWizardService`.
- Added focused init-wizard web/service tests for the new validation paths and validated them with `pytest tests/web/test_init_wizard.py tests/service/test_init_wizard.py`.
- Completed `P0 将下载失败结果回灌到 Wizard 主界面` by persisting sync failures into the step-5 wizard header and adding a dialog action that returns the user to the form for retry.
- Added focused step-5 download failure tests and revalidated init wizard coverage with `pytest tests/web/test_init_wizard.py tests/service/test_init_wizard.py`.
- Completed `P1 对齐 Init Wizard 完成跳转与 force 重初始化语义` by making completion always redirect to `/`, preserving `force=true` across wizard HTMX actions, and making fixed SQLite init idempotence explicit at the `init_data` boundary.
- Added regression coverage for force reconfiguration, root redirect semantics, persisted saved values, and fixed-db init deduplication; validated with `pytest tests/web/test_init_wizard.py tests/service/test_init_wizard.py tests/config/test_runtime.py`.
- Committed the completed P0/P1 work as `28c1950` with message `Tighten gateway gating and init wizard flow`, excluding the unrelated `.dev/rules/AGENTS.MD` and `.dev/specs/00-architecture.md` edits.
- Completed `P1 统一日线标准字段与类型` by normalizing daily-bar consumers and fetchers to `is_st`, casting `volume` to Float64 across the runtime/API path, and teaching daily-bar readers to transparently normalize legacy `st` data.
- Renamed the prepared parquet baselines from `st` to `is_st` in `tests/assets/2024_st_info.parquet` and `tests/assets/2024_bars_ext_cols.parquet` per the user's clarification.
- Refactored the affected data tests to run offline using local fixtures and a fake Tushare API, then validated the schema task with `pytest tests/data/fetchers/test_tushare.py tests/data/models/test_bars.py tests/data/models/test_stocks.py tests/service/test_grid_search.py`.
- Completed `P1 清理已退役的通知步骤残留` by removing `InitWizardService`'s unused notify default-loading and `save_notify_config()` entry point while keeping notify fields in `AppState` as runtime-owned configuration.
- Revalidated the wizard/runtime boundary with `pytest tests/service/test_init_wizard.py tests/config/test_runtime.py`.
- Completed `P2 引入标准数据源端口与适配器注册` by adding `DataFetcherPort`, a fetcher registry with `tushare` as the first adapter, and routing `StockSyncService`, `StockList`, `Calendar`, `DailyBarsStore`, and `LiveQuote` through the standard data-source seam.
- Added focused regression coverage for the registry, injected sync/store paths, and the updated calendar/stocks/livequote fetch seams; validated with `pytest tests/data/fetchers/test_registry.py tests/data/services/test_stock_sync.py tests/data/models/test_calendar.py tests/data/models/test_stocks.py tests/data/stores/test_base.py tests/data/stores/test_bars.py tests/service/test_livequote.py tests/data/fetchers/test_tushare.py`.
- Completed `P2 在初始化与运行时配置中绑定数据源` by persisting `data_source` in `AppState`, exposing it in init wizard step 5, reading it through `RuntimeConfig`, and making the fetcher registry resolve the current adapter from runtime state by default.
- Removed the wizard's remaining direct `tushare.set_token(...)` call by moving Tushare token configuration behind the adapter boundary, keeping background sync on the standard data API path.
- Completed the final regression suite with `pytest tests/web/test_init_wizard.py tests/service/test_init_wizard.py tests/web/test_trade.py tests/core/test_runtime_modes.py tests/config/test_runtime.py tests/data/fetchers/test_tushare.py tests/data/models/test_stocks.py tests/data/test_sqlite.py tests/data/fetchers/test_registry.py tests/data/services/test_stock_sync.py tests/data/models/test_calendar.py tests/data/stores/test_base.py tests/data/stores/test_bars.py tests/service/test_livequote.py`.
- Archived the fully completed `.dev/tasks.md` to `.dev/archived/26-04-01.tasks.md` and recreated a blank `.dev/tasks.md` for the next task batch.
