# 2026-04-10 对话记录

## 用户需求

1. 阅读 .dev 目录，分析项目文档优先级和功能欠缺
2. 修复 init-wizard 表单的边框和阴影问题
3. 修复 /data/market, /data/calendar 等 404 问题

## 分析结论

### 文档优先级

| 优先级 | 文档 | 说明 |
|--------|------|------|
| 最高 | specs/00-architecture.md | 技术栈和架构主决议 |
| 高 | specs/01-init-wizard.md | 初始化向导 |
| 高 | specs/02-layout-nav-style.md | 布局、导航、样式 |
| 高 | specs/03-system_management.md | 系统维护模块 |
| 中 | specs/three_mode_acceptance_checklist.md | 三模式验收清单 |
| 低 | archived/ | 历史文档，仅供参考 |

### 功能欠缺（到 03 文档为止）

1. **交易日历**：缺少箭头按钮、日期选择器、立即更新按钮
2. **股票列表**：缺少分页、250ms 防抖查询
3. **行情数据**：缺少分页、前复权按钮、日期范围过滤
4. **Header**：缺少 Alarm 通知图标、头像菜单"重设密码"
5. **计划任务/配置管理页面**：sidebar 有入口但页面未实现
6. **Live 模式端到端验收**：缺少真实 gateway 集成测试

## 修复内容

### 1. Init-wizard 表单样式

移除 Step2_Runtime, Step3_Admin, Step4_Gateway, Step5_DataSetup 中的 `Card(CardBody(...))` 包装，改为纯 `Div(...)`，去除边框和阴影。

提交：`💄 style: remove Card wrapper from init wizard forms`

### 2. Data 路由 404 问题

原因：Mount 的子应用路由定义在 `@rt("/")`，需要尾部斜杠才能匹配。

修复：在 app_factory.py 中添加重定向路由：
- `/data/calendar` -> `/data/calendar/`
- `/data/market` -> `/data/market/`
- `/data/stocks` -> `/data/stocks/`
- `/data/db` -> `/data/db/`

提交：`🐛 fix: add trailing slash redirects for data routes`
